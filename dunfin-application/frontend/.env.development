# Vite dev proxy target — used by vite.config.ts when VITE_API_URL is unset in dev
VITE_API_PROXY_TARGET=http://localhost:3000

# Production: set to your live API origin (e.g. https://api.example.com). Dev default is http://localhost:3000 via client.ts.
# VITE_API_URL=https://your-api.example.com
VITE_API_URL=https://iron-org.vercel.app/login
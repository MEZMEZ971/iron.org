# Vite dev proxy target — must match backend PORT in dunfin-application/backend/.env
VITE_API_PROXY_TARGET=http://localhost:3000

# Leave empty to use same-origin /api (proxied). Set only for direct API access.
# VITE_API_URL=

# Production API origin — required at build time (Vercel env or this file).
VITE_API_URL=https://ironorg-production.up.railway.app

# Public site origin (custom domain). Used for iOS web-clip generation and build-time references.
# At runtime, invite links prefer the browser origin via getWebAppOrigin().
# VITE_FRONTEND_URL=https://your-custom-domain.com
